var message = "hello coding";
let msg1 = "hello";
//** const = deklarasi variable konstan = tidak bisa diubah*/
//** let = deklarasi variable bersifat private (dalam curly bracketnya) */
const msg2 = "coding";
if (true) {
    var message = 1;
    let msg1 = 2;
}
console.log(message); 
console.log(msg1); 